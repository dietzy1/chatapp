version: "1.0"
 
services:
    postgres:
      image: postgres:latest
      environment:
        - POSTGRES_USER=root
        - POSTGRES_PASSWORD=root
        - POSTGRES_DB=postgres
      ports:
        - 5432:5432
    redis:
      image: redis
      restart: unless-stopped
      ports:
        - 9002:9002